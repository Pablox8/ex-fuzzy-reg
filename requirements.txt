# Core dependencies
# Preferred install: pip install -e .
# This file is for contributors who prefer: pip install -r requirements.txt
# Pinned floor versions tested during development (Python 3.11).

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
ex-fuzzy>=0.4
pymoo>=0.6

# Only needed if you call FuzzyVariable.validate().
# Declared as [project.optional-dependencies] validate in pyproject.toml.
# Uncomment or install separately with: pip install ex-fuzzy-reg[validate]
# scipy>=1.11

# Dev / testing
pytest>=7.0
pytest-cov>=4.0
